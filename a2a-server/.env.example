# A2A Server Configuration
# Single source of truth for all configuration values

# Container-internal configuration (these rarely change but are kept configurable for consistency)
A2A_SERVER_HOST=0.0.0.0
A2A_SERVER_PORT=10002

# External URL configuration (changes based on deployment)
A2A_AGENT_URL=http://host.docker.internal:10002/

# Logging Configuration
LOG_LEVEL=debug